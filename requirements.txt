# Pinned as requested: keep these two at the top
gradio==4.44.0
huggingface-hub==0.23.2

# Common ML / HF libraries (from your repo's existing requirements; adjust if unused)
torch>=2.0.0
torchaudio>=2.0.2
transformers>=4.30.0
accelerate>=0.20.3
safetensors>=0.3.2

# Audio helpers frequently used in demos
ffmpeg-python>=0.2.0
soundfile>=0.12.1
pydub>=0.25.1

# Utilities
numpy>=1.24.0
einops>=0.6.0
ftfy>=6.1.1
tokenizers>=0.13.3

# Optional / performance (uncomment if needed)
# xformers==0.0.20
# bitsandbytes==0.39.0